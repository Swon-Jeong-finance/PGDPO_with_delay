"""Table 1 and appendix tables, generated from outputs/results.csv only.
Contract: mean +- CI over seeds per (problem, method, metric); LaTeX booktabs
emission; column roster = control nRMSE, objective regret, KKT residual,
runtime (P3-D: paired dJ + violation instead of nRMSE). Filled when solver
runs land."""
